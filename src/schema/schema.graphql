# import * from 'person.graphql'
# import * from 'group.graphql'

scalar DateTime

type Query {
  q: String
}

type Mutation {
  test: Boolean
}

input CorePagination {
  from: Int
  size: Int
  sort: SortDirection
}

enum SortDirection {
  ASC
  DESC
}
